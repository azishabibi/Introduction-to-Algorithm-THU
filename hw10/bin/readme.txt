str.exe为可执行文件。如果想重新生成新的随机字符串，运行str_rand.exe，输入文本和模板的长度，生成的文件保存在input.txt中。
在运行str.exe前，如果要auto模式，请先确保input.txt中有对应的字符串。点击str.exe，输入auto将直接读取input.txt的内容，输入manual则需要手动输入模板和文本。输出内容为三种方法（暴力，kmp，bm）的开始匹配的位点（文本打头字符记为1，若没有匹配位点则没有此输出）和耗时。